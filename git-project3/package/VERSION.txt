DS-TH09-AI 3.5.0-test

Based on the accepted 3.4.0-test.
Reimu C1 uses observed enemy HP/activation, nonpiercing shots and bounded homing prediction.
Resource C1 revalidates its original ignition targets through charging.
AI side=1 requires a matching side_key; missing/wrong key falls back to 2P.
Public side_key is empty. Physical keyboard mapping and player ability settings are unchanged.
CSV: 169 columns; the first 162 columns are preserved.
